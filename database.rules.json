{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "reservations" : {
      "diner": {
        ".indexOn": "createdBy"
      },
      "bus": {
        ".indexOn": "createdBy"
      }
    }
  }
}
